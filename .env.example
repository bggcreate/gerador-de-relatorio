# =================================================================
# CONFIGURAÇÃO DO SISTEMA DE MONITORAMENTO
# =================================================================

# Ambiente de Execução
NODE_ENV=development

# Porta do Servidor
PORT=5000

# =================================================================
# SEGURANÇA - SECRETS (OBRIGATÓRIOS EM PRODUÇÃO)
# =================================================================

# Secret para sessões (gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=sua_chave_secreta_aqui_minimo_64_caracteres

# Secret para tokens JWT (gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=sua_chave_jwt_secreta_aqui_minimo_64_caracteres

# =================================================================
# ACESSO TEMPORÁRIO DE DESENVOLVIMENTO (APENAS PARA DEV/TESTE)
# =================================================================

# ATENÇÃO: Esta funcionalidade fornece acesso completo de desenvolvedor (role: dev)
# sem necessidade de login tradicional, usando tokens JWT temporários.
#
# IMPORTANTE:
# 1. Apenas habilite em ambiente de desenvolvimento/teste
# 2. NUNCA habilite em produção
# 3. Tokens têm validade configurável (padrão: 1 hora)
# 4. Todos os usos são registrados em audit log
# 5. Tokens podem ser revogados manualmente a qualquer momento
#
# Para habilitar: defina como 'true' E certifique-se que NODE_ENV=development
DEV_TEMP_ACCESS=true

# =================================================================
# GOOGLE DRIVE - ARMAZENAMENTO GRATUITO (15GB)
# =================================================================

# Obtenha as credenciais em: https://console.cloud.google.com/
# Siga o guia: GOOGLE_DRIVE_SETUP.md

# ID do cliente OAuth 2.0
GOOGLE_CLIENT_ID=seu_client_id.apps.googleusercontent.com

# Secret do cliente OAuth 2.0
GOOGLE_CLIENT_SECRET=GOCSPX-seu_client_secret_aqui

# Refresh Token (obtenha com: node scripts/google-auth-setup.js)
GOOGLE_REFRESH_TOKEN=1//0seu_refresh_token_aqui

# Email para enviar backups automáticos
EMAIL_REMETENTE=seu_email@gmail.com
EMAIL_BACKUP=seu_email@gmail.com

# =================================================================
# BANCO DE DADOS
# =================================================================

# OPÇÃO 1: SQLite (Local - Padrão)
# Caminho do banco de dados SQLite (opcional, padrão: data/database.db)
# DB_PATH=database.db

# OPÇÃO 2: PostgreSQL na Nuvem (Tembo.io - Recomendado)
# Para usar PostgreSQL gratuito do Tembo.io, configure as variáveis abaixo:
#
# 1. Crie uma conta grátis em: https://cloud.tembo.io
# 2. Crie uma instância PostgreSQL (10GB gratuito no Hobby Tier)
# 3. Copie as credenciais fornecidas abaixo:

# Host da instância Tembo (exemplo: seu-projeto.data-1.use1.tembo.io)
PGHOST=seu-host.data-1.use1.tembo.io

# Porta PostgreSQL (padrão: 5432)
PGPORT=5432

# Nome do banco de dados (padrão: postgres)
PGDATABASE=postgres

# Usuário PostgreSQL (padrão: postgres)
PGUSER=postgres

# Senha do PostgreSQL (fornecida pelo Tembo)
PGPASSWORD=sua_senha_postgresql_aqui

# Modo SSL (obrigatório para Tembo: require)
PGSSLMODE=require

# Nome desta instância (para identificação em multi-computador)
INSTANCE_NAME=Computador Principal

# IMPORTANTE: Para migrar do SQLite para PostgreSQL, execute:
# node scripts/migrate-to-postgres.js
#
# Consulte o guia completo em: docs/GUIA_RAPIDO_POSTGRESQL.md

# =================================================================
# INSTRUÇÕES DE SEGURANÇA
# =================================================================

# 1. NUNCA commite este arquivo com secrets reais no git
# 2. Use .env para desenvolvimento local (já está no .gitignore)
# 3. Em produção, use variáveis de ambiente do servidor/plataforma
# 4. Gere secrets fortes e únicos para cada ambiente
# 5. Desabilite DEV_TEMP_ACCESS antes de qualquer deploy em produção

# Para gerar secrets seguros, execute:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
